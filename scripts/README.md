# Scripts

Anz Clicker stores user-created script JSON files in this directory by default.

Script JSON files are intentionally ignored by Git so personal automation scripts are not committed accidentally.
